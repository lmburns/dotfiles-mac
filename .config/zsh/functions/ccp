# desc: preserve xattr for gnu copy

zparseopts -D -A opts 'p' '-preserve'

if (( ${+opts[-p]} )) {
  /bin/cp -piv "$@"
} else {
  cp -iv "$@"
}

# vim: ft=zsh:et:sw=0:ts=2:sts=2:fdm=marker:fmr={{{,}}}:
