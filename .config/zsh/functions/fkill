# desc: interactively kill process with fzf

ppkill() {
  px \
    | sed 1d \
    | awk 'BEGIN{OFS="\t"}{print $1, $2, $NF}' \
    | fzf --height=70% \
    | awk '{print $1}' \
    | xargs -I{} kill -KILL "{}"
}

ppkill

# vim:ft=zsh:
