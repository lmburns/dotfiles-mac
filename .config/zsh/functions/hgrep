# desc: grep history

hgrep() {
  local cmd
  zparseopts -D -a opt 'f' '-fzf'
  cmd='fc -lnr 1 | rg "${@}"'
  if [[ "${opt[1]}" == -(f|-fzf) ]] cmd="${cmd} | fzf"
  eval "$cmd"
}

hgrep "$@"

# vim:ft=zsh:et:sts=2:sw=0
